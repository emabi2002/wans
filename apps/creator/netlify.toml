# Netlify Configuration for THE WANS Creator Portal
# Next.js 15 Deployment

[build]
  base = "."
  command = "cd ../.. && bun install && cd apps/creator && bun run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.0.0"
  NODE_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
